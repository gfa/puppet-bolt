---
concurrency: 1
format: json

apply_settings:
  show_diff: true

log:
  console:
    level: warn
  logs/debug.log:
    level: debug
    append: false
  bolt-debug.log: disable

inventoryfile: hieradata/inventory.yaml
