---
concurrency: 1
format: human

log:
  console:
    level: debug
  logs/debug.log:
    level: debug
    append: true

ssh:
  user: root
  host-key-check: false

inventoryfile: hieradata/inventory.yaml
