---
concurrency: 1
format: human

log:
  console:
    level: info
  logs/debug.log:
    level: debug
    append: false

ssh:
  user: root
  host-key-check: false

inventoryfile: hieradata/inventory.yaml
