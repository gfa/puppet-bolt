---
concurrency: 1
format: human

log:
  console:
    level: warn
  logs/debug.log:
    level: debug
    append: false

ssh:
  user: root
  host-key-check: false
  native-ssh: true

inventoryfile: hieradata/inventory.yaml
