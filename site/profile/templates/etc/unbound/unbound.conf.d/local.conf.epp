server:
        interface: 127.0.0.1
        <%- $::profile::networking::unbound::access_control.each |$client| { %>
        access-control: <%= $client -%>
        <% } %>
        port: 5553

remote-control:
        control-enable: no
