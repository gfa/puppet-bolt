# managed by puppet

router id <%= @ipv4_own %>;

define OWNAS =  <%= @as %>;
define OWNIP = <%= @ipv6_own %>;

function is_self_net() {
  return net ~ [<%= @ipv6_range %>+];
}

function is_valid_network() {
  return net ~ [
    fd00::/8{44,64} # ULA address space as per RFC 4193
  ];
}
